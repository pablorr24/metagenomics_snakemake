#This rule generates a citation document for the long-reads workflow
rule citation:
    input: "{wd}/citations/nanoplot_citation.txt"
    output: "{wd}/output/long_reads/{rs}/nanoplot_citation.txt"
    shell: "cp {input} {output}"
